"""Native Python DSST Monte Carlo uncertainty propagation.

The CLI and public simulation API share the same implementation. Importing the
package does not load an external DSST checkout or start worker processes.
"""

__version__ = "0.1.0"
